% concept: ham-dac-trung-tap-hop
\textbf{Hàm đặc trưng (indicator) $\chi_A$.}
$\chi_A(x)=1$ nếu $x\in A$ và $=0$ nếu $x\notin A$; dùng để biểu diễn dữ liệu/nguồn gián đoạn có giá compact (khác 0 chỉ trong vùng hữu hạn).
